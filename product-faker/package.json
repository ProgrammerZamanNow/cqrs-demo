{
  "name": "product-faker",
  "version": "1.0.0",
  "description": "Generate fake brands, categories & products via the product-backend API",
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "start": "bun run index.ts",
    "generate": "bun run index.ts"
  },
  "dependencies": {
    "@faker-js/faker": "^9.3.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
